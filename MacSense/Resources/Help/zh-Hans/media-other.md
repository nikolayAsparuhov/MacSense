# 其他

不属于任何命名媒体桶的文件 — 缓存、应用数据、部分下载、无扩展名文件和专有格式。

## 详情

这是“其他一切”的桶。一个大的“其他”总数往往指向:

- 应用支持文件夹(`~/Library/Application Support/...`)。
- 后缀为 `.part` / `.crdownload` 的部分下载。
- 从 `.dmg` 文件挂载的磁盘映像。
- 沙盒容器和组容器。

删除前先调查较大的“其他”条目 — 有些属于你正在使用的应用。
