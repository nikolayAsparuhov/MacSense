# 用户缓存

存放在 `~/Library/Caches` 中的应用生成数据。应用将其视为可丢弃存储 — 丢失后只会让下次启动稍慢,绝不会损坏任何东西。

## 详情

每个应用使用缓存来存:

- 预渲染图像、缩略图和已解码的媒体。
- 应用可重新获取的已下载资源包。
- 搜索索引和最近历史记录。

现代 Mac 会随时间累积数 GB,尤其是浏览器、媒体应用和基于 Electron 的工具。清理后的首次启动稍慢,之后恢复正常。

MacSense 跳过仍在使用的缓存(已锁定文件、当前打开的数据库),其余移入废纸篓,从不直接删除。
