# 压缩包

压缩归档、磁盘映像和安装包。通常已过时 — 在解压内容或运行安装器后留下。

## 详情

被归为压缩包的扩展名:`zip`、`tar`、`gz`、`tgz`、`bz2`、`xz`、`7z`、`rar`、`dmg`、`iso`、`pkg`、`jar`、`war`、`deb`、`rpm`。

`~/Downloads` 中的 `.dmg` 和 `.pkg` 文件是特别常见的“嫌犯” — 即使你已将应用移到“应用程序”后,大多数安装器仍会把原文件留在那里。
