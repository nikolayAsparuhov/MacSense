# 系统垃圾

macOS 或已安装应用在主目录之外留下的日志、临时文件和崩溃报告。可安全删除 — 系统会重新生成所需内容。

## 详情

涵盖三类:

- **诊断报告** 位于 `/Library/Logs` 和 `/var/log`。macOS 自动轮转。
- **崩溃报告** 位于 `/Library/Application Support/CrashReporter`。崩溃后立即有用,几个月后无用。
- **临时安装产物** 由软件包安装器和应用更新留下。

MacSense 将所有内容移到废纸篓。这里没有你的文档或偏好设置 — 仅是系统生成的产物。
