# 废纸篓

`~/.Trash` 的内容以及任何卷级别的废纸篓(外置驱动器)。MacSense 清空它们,空间真正归还给系统。

## 详情

将文件拖到废纸篓只是标记为待删除 — 字节会留在磁盘上,直到清空。macOS 在磁盘工具中显示已回收的空间,但在确认前一直视为占用。

MacSense 的废纸篓清扫:

- 列出 `~/.Trash` 和每个已挂载卷上的所有文件。
- 报告总可恢复大小。
- 清理时永久删除文件。

与其他类别不同,这是 MacSense 唯一的**不可逆**清理 — 一旦清空,文件就消失了。确认前先看一下列表。
