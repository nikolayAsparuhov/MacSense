# 视频

MacSense 在你的主目录下找到的所有视频文件。通常是 Mac 上最大的单个类别,因为视频比特率远超其他内容。

## 详情

被归为视频的扩展名:`mp4`、`mov`、`mkv`、`avi`、`webm`、`m4v`、`flv`、`wmv`、`mpeg`、`mpg`、`3gp`、`ts`。

卡片显示 5 个最大的视频及其磁盘占用大小。点击卡片打开完整列表,从大到小排序,你可以将单个文件移到废纸篓。

System Library 视频资源和受保护应用 bundle 内的项目被排除。
